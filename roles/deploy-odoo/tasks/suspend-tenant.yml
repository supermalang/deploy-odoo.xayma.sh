---
- name: Suspend the tenant (REVOKE DB access, purge sessions, repoint IngressRoute at the suspended page)
  ansible.builtin.include_tasks: _set-tenant-state.yml
  vars:
    _state_target: suspended
