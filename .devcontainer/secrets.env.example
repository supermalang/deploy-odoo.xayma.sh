# Copy this file to secrets.env and fill in the real values.
# secrets.env is gitignored and mounted into the dev container as env vars.

# Claude Code auth token (from `claude setup-token`)
CLAUDE_CODE_OAUTH_TOKEN=

# GitHub Personal Access Token (used by the gh CLI and git)
GH_TOKEN=
